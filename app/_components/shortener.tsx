import LinkForm from "@/subcomponents/link-form";

const Shortener = () => {
    return (
        <div className="mt-14 sm:mt-20 flex justify-center items-center p-4 w-full h-[82vh]">
            <LinkForm />
        </div>
    );
};

export default Shortener;
